# Backend server port
PORT=4000

# Ethereum RPC endpoint (Better need to use Sepolia)
RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Address of the deployed smart contract
CONTRACT_ADDRESS=0x...

# Private key used by the backend for signing (only test account)
PRIVATE_KEY=0x...

# Email receivers (any email address for guardian and owner roles)
GUARDIAN_EMAIL=guardian@example.com
OWNER_EMAILS=owner1@example.com,owner2@example.com

# Gmail SMTP settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=user@gmail.com
SMTP_PASS=app_password
MAIL_FROM="Delayed MultiSig <user@gmail.com>"

FRONTEND_URL=http://localhost:3000
